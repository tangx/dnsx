version: '3.1'

services: 
  postgres:
    restart: always
    image: postgres:11
    ports:
      - 5432:5432
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: Abc123123
    network_mode: default
